@model TLBD.Areas.admin.Models.Account.LogOnModel

@{
    ViewBag.Title = "Log On";
}

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>
<link href="@Url.Content("~/css/Customs/style.css")" rel="stylesheet" />
<link href="@Url.Content("~/css/Customs/animate-custom.css")" rel="stylesheet" />

<div style="min-height:20px; clear:both;"></div>

@*<div>
        <div style="text-align:center;color:red;">@Html.ValidationSummary(true, "Đăng nhập không thành công. Xin vui lòng kiểm tra thông tin và đăng nhập lại.")</div>

        @using (Html.BeginForm()) {
            <div>
                <fieldset>
                    <legend>Thông tin đăng nhập</legend>

                    <div class="editor-label">
                        @Html.LabelFor(m => m.UserName)
                    </div>
                    <div class="editor-field">
                        @Html.TextBoxFor(m => m.UserName)
                        @Html.ValidationMessageFor(m => m.UserName)
                    </div>

                    <div class="editor-label">
                        @Html.LabelFor(m => m.Password)
                    </div>
                    <div class="editor-field">
                        @Html.PasswordFor(m => m.Password)
                        @Html.ValidationMessageFor(m => m.Password)
                    </div>

                    <div class="editor-label">
                        @Html.CheckBoxFor(m => m.RememberMe)
                        @Html.LabelFor(m => m.RememberMe)
                    </div>

                    <p>
                        <input type="submit" value="Đăng nhập" />
                    </p>
                </fieldset>
            </div>
        }
    </div>*@

<div class="container" style="margin-top:5px;">
    <section>
        <div id="container_demo">
            <div id="wrapper">
                <div id="login" class="animate form">
                    @using (Html.BeginForm())
                    {
                        <div style="text-align:center;"><h2 style="color:blue;">ĐĂNG NHẬP</h2></div>
                        @Html.ValidationSummary(true, "", new { style = "color:red" })
                        <p>
                            <label for="username" class="uname" data-icon="u"> Tên đăng nhập </label>
                            @Html.TextBoxFor(m => m.UserName, new { @id = "username", @required = "required", @type = "text", @placeholder = "Tên đăng nhập" })
                            @Html.ValidationMessageFor(m => m.UserName)
                        </p>
                        <p>
                            <label for="password" class="youpasswd" data-icon="p"> Mật khẩu </label>
                            @Html.TextBoxFor(m => m.Password, new { @type = "password", @id = "password", @placeholder = "Mật khẩu", @required = "required" })
                            @Html.ValidationMessageFor(m => m.Password)
                        </p>
                        <p class="keeplogin">
                            @Html.CheckBoxFor(m => m.RememberMe, new { id = "loginkeeping" })
                            @*<input type="checkbox" name="loginkeeping" id="loginkeeping" value="loginkeeping" />*@
                            >><label for="loginkeeping">Ghi nhớ</label>
                        </p>
                        <p class="login button">
                            <input type="submit" value="Đăng nhập" />
                        </p>
                        <p class="change_link">
                            <a href="@Url.Action("Index", "Home", new{@area=""})">Quay về trang chủ</a>
                        </p>
                    }
                </div>
            </div>
        </div>
    </section>
</div>